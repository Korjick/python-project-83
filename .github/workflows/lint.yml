name: lint

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Install dependencies
        run: make install
      - name: Run linter
        run: make lint
